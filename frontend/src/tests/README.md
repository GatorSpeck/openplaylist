# Frontend Tests

This folder contains frontend tests, fixtures, and test utilities.

## Structure

- `PlaylistEntry.test.jsx`: component behavior test example
- `components/`: component-focused tests
- `test-utils.js`: shared rendering/test helpers
- `mockData.js`: reusable fixture data

## Running

From `frontend/`:

```bash
npm test
```
