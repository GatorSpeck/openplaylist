# React Contexts

This folder contains application-wide React context providers.

## Current Context

- `ThemeContext.tsx`: theme mode state and related helpers

## Notes

- Add context only for state that truly needs broad tree-wide access.
- Prefer component-local state for narrow concerns.
