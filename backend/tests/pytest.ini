[pytest]
asyncio_default_fixture_loop_scope="function"
markers =
    live: hits real Plex/Spotify/YouTube accounts using credentials from .env/oauth.json; run explicitly with `pytest -m live`
addopts = -m "not live"